[project]
name = "nexus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ngo Tuan Minh", email = "ngominh160309@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "google-genai>=2.21.0",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.3",
    "python-multipart>=0.0.32",
    "streamlit>=1.63.0",
    "supabase>=2.31.0",
]

[project.scripts]
nexus = "nexus:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
